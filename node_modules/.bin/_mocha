#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../mocha/bin/_mocha" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../mocha/bin/_mocha" "$@"
  ret=$?
fi
exit $ret
